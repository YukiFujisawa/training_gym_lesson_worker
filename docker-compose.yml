version: '3'
services:
  db:
    image: mysql:8.0.14
    ports:
      - 3306:3306
    volumes:
      - ~/docker/mysql/config:/etc/mysql/conf.d
      - ~/docker/mysql/data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
